// Program that works under Tree Borrows but fails under Stacked Borrows
fn reordered_reads() {
    let _root: i32 = 42;

    todo!("Implement reordered reads");
}

fn main() {
    reordered_reads();
}
